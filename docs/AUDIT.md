# Repository audit (E0.1)

Generated by inspection against `docs/spec/*.md` v6. This file is the source of truth for
E0.5 ledger reconciliation. Where this document and the ledger disagree, this document —
because it was produced by reading the actual repository — wins until the ledger is updated
in E0.5.

## Commands that actually run

| Command | What it does | Result at audit time |
|---|---|---|
| `npm install` | installs deps (npm, not bun, despite `bun.lock` being present) | OK |
| `npm run dev` | `tsx server.ts` — Express + Vite middleware dev server | not exercised in this audit (no browser) |
| `npm run build` | `vite build && esbuild server.ts ...` → `dist/` | OK, 3 harmless `import.meta` CJS warnings (see REFACTOR note on `server.ts`) |
| `npm run start` | `node dist/server.cjs` | not exercised |
| `npm run lint` | `tsc --noEmit` | OK, zero errors |
| `npm run test` | `tsx --test tests/**/*.test.ts` (Node's built-in test runner, **not Vitest** as `01_ARCHITECTURE.md` specifies) | **23/23 pass, 0 fail, 0 skip** |
| `npm run test:all` | lint + test + build | **green** |
| `npm run demo:jh16` | required by `00_CZYTAJ_TO_NAJPIERW.md` / E1.24 | **does not exist** |
| `npm run clean` | `rm -rf dist server.js` | OK |

Stack deviations from `01_ARCHITECTURE.md` (noted, not "fixed" — D1 in `00_STATE_AND_DECISIONS.md`
says adapt the doc to the repo, not the reverse):

- Test runner is `node:test` via `tsx`, not Vitest.
- Backend framework is Express, not Fastify (spec explicitly allows "or the framework already
  present in the repo").
- Two lockfiles coexist: `package-lock.json` (npm, what `npm install`/CI actually uses) and
  `bun.lock` (stale, nothing in scripts/docs invokes bun). One should be deleted; flagged here,
  not deleted in E0.1 since deleting a lockfile is a REFACTOR-scale decision for E0.5/E1.1, not
  an inventory action.

## Full source tree with verdicts

Legend: **KEEP** = correct and matches or cleanly maps to spec intent, keep as-is for now.
**REFACTOR** = keep the logic, reshape it (rename, relocate, extend) to match spec contracts.
**REPLACE** = the concept must exist per spec but this implementation must be substantially
rewritten. **DELETE** = remove. Nothing below is deleted in E0.1; deletions happen in the epic
that owns the file.

### Root

| File | Verdict | Reason |
|---|---|---|
| `package.json` | REFACTOR | scripts/deps fine; needs `demo:jh16`, and unused deps removed (see below) |
| `package-lock.json` | KEEP | canonical lockfile — matches what scripts actually use |
| `bun.lock` | DELETE (defer to E0.5) | nothing invokes bun; a second lockfile is a source of drift |
| `tsconfig.json` | KEEP | strict-enough for E1; will need `src/domain` etc. path additions later |
| `vite.config.ts` | KEEP | frontend build config, fine; AI-Studio-specific `DISABLE_HMR` comment is harmless leftover |
| `server.ts` | REFACTOR | Express app + manual CORS + Vite middleware; works, but `api/`-layer boundary (`01_ARCHITECTURE.md`) says HTTP transport belongs in `api/`, not the app root; the `import.meta`-in-CJS warnings on build are cosmetic but should be cleaned when this file is touched |
| `index.html` | KEEP | Vite entry, unremarkable |
| `metadata.json` | KEEP | AI-Studio app descriptor, not read by our app; harmless |
| `.env.example` | REFACTOR | only has AI-Studio's `GEMINI_API_KEY`/`APP_URL`; needs real E1 secret names once any exist (none needed until E3) |
| `.gitignore` | KEEP | now covers `data/` (fixed last session) |
| `assets/.aistudio/.gitignore` | KEEP | AI-Studio scaffold marker, ignores itself, inert |
| `00_CZYTAJ_TO_NAJPIERW.md` | KEEP | maintainer-facing README for this spec package |
| `CLAUDE.md` | KEEP | agent operating contract, authoritative |
| `PROMPT_CLAUDE_CODE.txt` | KEEP | session bootstrap prompt |

### `docs/`

| Path | Verdict | Reason |
|---|---|---|
| `docs/spec/*.md` (00–09) | KEEP | binding v6 contract, highest precedence per its own §5 |
| `docs/specifications/*` (v3 package + config/schemas/contracts examples) | KEEP, demoted | per `00_STATE_AND_DECISIONS.md` §2: "superseded on stack, auth, providers; retained for scope, data model, source catalogue, diagnostics contract" — kept as reference, `docs/spec/` governs on conflict |
| `docs/architecture/ADR-001-Tech-Stack-Adaptation.md` | KEEP | historical decision record, consistent with D1 |
| `docs/architecture/ADR-002-Local-Persistence-Sandbox.md` | KEEP | consistent with D2 |
| `docs/architecture/ADR-003-Autonomous-Replication-Agent.md` | KEEP | consistent with D9/E5 |

### `backend/watchdog_api/` (current backend root — target per spec is `src/{api,services,domain,analysis,adapters,repo,diag}`)

| File | Verdict | Reason |
|---|---|---|
| `analytics/base.ts` | REFACTOR | `Analyzer`/`StatisticalResult` types; conceptually becomes part of `src/analysis`, but the *interface shape* (bespoke analyzer, not `MethodSpec`+primitives) must be REPLACED per D6/E1.11-15 |
| `analytics/jh16.ts` | REPLACE | correct arithmetic (`Pi`/`Hi` verified against `03_JH2016_CONTRACT.md` last session) but implemented as bespoke TypeScript, not a `MethodSpec` over the primitive registry. The *numbers* are trustworthy; the *shape* is not what E1.15 requires. |
| `analytics/registry.ts` | REFACTOR | simple analyzer registry; becomes provider/primitive-aware per `05_PROVIDERS_AND_CAPABILITIES.md` |
| `analytics/stats.ts` | KEEP | `pearson`/`spearman`/`calculateRatio`/`normalizeMax` are correct, pure, already return `null` (not `0`) on undefined cases (fixed last session) — these become the bodies of the `pearson`/`spearman`/`ratio`/`max` primitives in `04_METHOD_COMPILER_AND_APPROVAL.md`'s registry almost unchanged |
| `api/middleware.ts` | KEEP | trace + error-handling middleware, aligns with `06_DIAGNOSTICS.md` intent (see Diagnostics section below) |
| `api/routes.ts` | REFACTOR | works, but has no `owner_principal_id`/`visibility`/`IdentityProvider` per D3, no approval-gate enforcement per D7 |
| `api/schemas.ts` | REFACTOR | one Zod schema for run submission; fine as a start, needs to grow with MethodSpec/approval contracts |
| `config/canonicalize.ts` | KEEP | canonical JSON + SHA-256 hash, exactly what E1.1 needs; test already proves key-order independence |
| `config/loader.ts` | KEEP | deep-merge config layers + locked-preset enforcement; this **is** a working start on E1.1, just not wired into the running app (`server.ts`/`routes.ts` never call it) |
| `config/schemas.ts` | REFACTOR | Zod schemas for app/source/preset config; `AnalysisPresetSchema` shape (`entities`+`query_templates`) differs from spec's `config/presets/jh2016-faithful.json` shape (`substances[{canonical,query_label}]`+`queries.{popularity_template,harm_template}`) — needs reconciling in E1.1 |
| `db/client.ts` | REFACTOR | inline `CREATE TABLE IF NOT EXISTS` DDL, not a migration system; works for E1 scale but `02_DATA_MODEL.md`'s much larger schema needs real migrations |
| `db/repositories/acquisition.ts` | KEEP | content-addressed blob dedup + distinct fetch-event history already implemented and tested (`persistence.test.ts`) — matches `02_DATA_MODEL.md`'s "identical bytes deduplicated, fetch events stay distinct" requirement |
| `db/repositories/artifacts.ts` | KEEP | WORM-enforcing manifest repo, tested; becomes `ManifestRepo` |
| `db/repositories/data.ts` | REFACTOR | `ObservationRepository.getByRunId` hardcodes `source_adapter_version: 'unknown'` on read (line 36) because the column isn't in the `observations` table — a real provenance gap flagged for E0.2/E1.3, not a numeric fabrication but adjacent to one |
| `db/repositories/runs.ts` | REFACTOR | run state machine here is `CREATED/QUEUED/ACQUIRING/NORMALIZING/ANALYZING/SUCCESS/FAILED`; spec's is `CREATED→VALIDATING→QUEUED→RUNNING→NORMALIZING→ANALYZING→EXPORTING→COMPLETED(+FAILED/CANCELLED)` — different vocabulary, needs reconciling once, not per-task |
| `db/schema.ts` | REPLACE | current 6 tables (`runs, raw_blobs, fetch_events, manifests, observations, analysis_results`) are a fraction of `02_DATA_MODEL.md`'s ~25 tables; no `owner_principal_id`/`visibility` anywhere, no `principals`, `substances`, `capabilities`/`providers`/`credentials`, `series`, `method_specs`, `replication_*`, `datasets`/`transforms`, `audit_events` |
| `services/run_orchestrator.ts` | REFACTOR | single fetch-per-run orchestration; correct graceful-failure behaviour (fixed last session — catches, marks FAILED, does not rethrow) but no approval-gate check (D7), no `MethodSpec` execution, no multi-entity FAITHFUL-preset acquisition loop (32 queries/run) |
| `sources/base.ts` | REFACTOR | `SourceAdapter` interface is structurally close to spec's (`fetch/normalize/provenance/validate`) but `snake_case` methods, `capabilities()` plural vs spec's `capability()` singular |
| `sources/google_trends.ts` | KEEP | added last session; fixture-status, deliberately uses `interest_index` dimension (not `popularity`/`harm`) per the explicit anti-conflation warning in the archive decisions — matches D4-style substitution rule ("no silent... substitution") |
| `sources/offline_fixture.ts` | REFACTOR | works and is tested, but its fixture data is hardcoded inline in the `.ts` file, not read from `fixtures/jh2016/*.json` as `01_ARCHITECTURE.md`/E1.8/E1.9 require |
| `sources/registry.ts` | REPLACE | conflates Source and Provider (D5 says these are orthogonal); no `ProviderRegistry`/`CredentialRegistry`, no `proposed`→approved discovery flow |
| `sources/serp.ts` | KEEP | fixture-status, mocked, harm-detection restored last session, tested — legitimate stand-in for the eventual `search.result_count` provider (E3), correctly *not* claiming to be live |
| `storage/client.ts` | KEEP | thin composition-root wiring for the object store |
| `storage/object_store/index.ts` | KEEP | WORM-enforcing content-addressed local blob store, tested — this **is** `02_DATA_MODEL.md`'s blob store requirement, already correct |
| `utils/errors.ts` | KEEP | `ErrorEnvelope` + cause-chain builder, matches `06_DIAGNOSTICS.md` error-taxonomy intent closely; now also exports `NotImplementedError` (added E0.2) |
| `utils/redaction.ts` | REFACTOR | works and is tested (canary-style assertion in `tracer.test.ts`), but the sensitive-key list is small (`password, token, secret, api_key, apikey, authorization, client_secret`) — `06_DIAGNOSTICS.md` requires a redaction layer that "knows every credential-shaped key"; needs the canary-secret CI test called out explicitly in `09_TESTS.md`, not just an assertion embedded in another test |
| `utils/tracer.ts` | KEEP | this is a real, working flight recorder: modes (`OFF/ERRORS/NORMAL/TRACE`), `AsyncLocalStorage` correlation context, `trace_id`/`span_id`/`parent_span_id`/`sequence_no`, JSONL persistence, redaction on every write. Substantially satisfies E1.4–E1.6 already. Gaps vs spec: no `STATE_BEFORE→INPUT→VALIDATION→DECISION→...` full vocabulary (has a subset), no diagnostic bundle (E1.7), no Debug Console. |

### `src/` (frontend — target layout matches spec's `ui/` almost exactly already)

| File | Verdict | Reason |
|---|---|---|
| `App.tsx` | KEEP | router shell, fine |
| `components/Layout.tsx` | KEEP | nav shell, fine |
| `index.css` | KEEP | Tailwind entry |
| `lib/api.ts` | KEEP | typed fetch wrappers, fine |
| `lib/utils.ts` | KEEP | `cn()` helper, fine |
| `main.tsx` | KEEP | Vite/React entry |
| `pages/Analyzers.tsx` | REFACTOR | lists analyzers/runs analysis; needs approval-state UI per D7 once method compiler exists |
| `pages/Dashboard.tsx` | KEEP | run status overview |
| `pages/RunDetails.tsx` | REFACTOR | shows observations/results; needs manifest fields, quality flags, `PROPOSED`/`APPROVED` visual distinction (D7) |
| `pages/Runs.tsx` | KEEP | run list |
| `pages/Sources.tsx` | REFACTOR | today lists sources with a `status`/`adapter` shape from `registry.listSources()`; once Source/Provider split lands (D5), this page must stop rendering providers entirely |
| `types.ts` | REFACTOR | `Run.status` enum (`QUEUED/ACQUIRING/NORMALIZING/ANALYZING/SUCCESS/FAILED`) needs to gain `CREATED/VALIDATING/RUNNING/EXPORTING/COMPLETED/CANCELLED` per spec's lifecycle, in the same pass as `db/repositories/runs.ts` |

### `tests/`

| File | Verdict | Reason |
|---|---|---|
| `contract/sources.test.ts` | KEEP | exercises all 3 adapters + registry gating of `planned` status; becomes the seed of the E1.8 `SourceAdapter` conformance suite `09_TESTS.md` asks for |
| `e2e/api_flow.test.ts` | REFACTOR (fixed E0.4) | was an empty file that `node:test` silently reported as a zero-assertion pass; replaced with an explicit `test(..., { skip: '<reason>' }, ...)` so the gap shows up as 1 skipped test instead of a phantom green. Becomes the real E1.21-23 browser E2E test once those pages exist. |
| `integration/api.test.ts` | KEEP | full HTTP flow through `server.ts`'s `app`, tests source list, analyzer list, run submission, polling, results, fetch-events, raw artifact retrieval — good coverage of what exists |
| `integration/orchestrator.test.ts` | KEEP | exercises graceful-failure path and evidence retention, correctly updated last session |
| `integration/persistence.test.ts` | KEEP | blob dedup + WORM tests, both directions (manifest and object store) |
| `scientific/jh16.test.ts` | KEEP | golden values for `Pi`/`Hi` and correlation presence; becomes the seed of the E1.15 golden-fixture suite, though it doesn't yet cover the full `03_JH2016_CONTRACT.md` test list (missing count, zero `Ni`, count-string parsing, faithful immutability, exact-32-query golden) |
| `unit/anti_fabrication.test.ts` | KEEP | added E0.2; the anti-fabrication test `09_TESTS.md` requires from E0.2 |
| `unit/config.test.ts` | KEEP | canonicalization, precedence, lock enforcement — real coverage of E1.1 |
| `unit/tracer.test.ts` | KEEP | context propagation, redaction, cause chains — real coverage of E1.4-E1.6 |

## Unused declared dependencies

None of these are imported anywhere in `backend/`, `src/`, or `server.ts`:

| Dependency | Kind | Note |
|---|---|---|
| `@google/genai` | dependency | AI-Studio scaffold leftover; correctly *not* used yet — D6/rule 2 says no LLM in the numerical path, and the method compiler (E2) hasn't been built. Keep declared or remove is an E0.5/E1 call, not urgent. |
| `jstat` | dependency | unused; `analytics/stats.ts` reimplements pearson/spearman/ratio itself instead |
| `motion` | dependency | unused animation library |
| `uuid` | dependency | unused — code uses `node:crypto`'s `randomUUID()` everywhere instead |
| `recharts` | dependency | unused — no chart is rendered yet (E1.16 not started) |
| `drizzle-kit` | devDependency | unused — no `drizzle.config.ts`, no `drizzle-kit` script; migrations are done by hand in `db/client.ts` |

`cors` and `dotenv` are also declared but unused: `server.ts` sets CORS headers manually instead
of using the `cors` middleware, and nothing calls `dotenv.config()` (env vars are read directly
via `process.env`).

## TODO / FIXME / stub scan

`grep -rnE "TODO|FIXME|XXX|HACK|not implement|NotImplementedError|stub"` across `backend/`,
`src/`, `server.ts` returns **zero matches**. There is no explicit stub marker anywhere in the
codebase. This is good news with one caveat feeding into E0.2: absence of a `TODO` marker is not
the same as absence of a fabrication — see the anti-fabrication sweep for the one real finding
(`data.ts`'s hardcoded `'unknown'` provenance string on read).

## Green baseline (E0.4)

`npm run test:all` exits 0: **25 passing, 1 explicitly skipped, 0 failing**, `tsc --noEmit`
clean, `vite build` succeeds (3 harmless `import.meta`-in-CJS warnings from `esbuild` bundling
`server.ts` to `dist/server.cjs`, pre-existing, cosmetic only — `server.ts` isn't imported as
CJS at runtime in a way that path executes).

The one skip, with reason, per E0.4's "explicitly marking and listing skipped tests" clause:

| Test | Reason skipped |
|---|---|
| `tests/e2e/api_flow.test.ts` — "E2E: full fixture run driven from the Study/Method Review/Results pages" | Requires E1.21-23 (those UI pages don't exist yet) and a browser driver. Was previously an empty file silently counted as a zero-assertion pass (see E0.1 finding); now an explicit skip so the gap is visible in `npm run test` output rather than hidden. |

No other test in the suite is skipped, marked `.todo`, or conditionally disabled.

## Secrets scan (E0.3)

Scanned `git log --all -p` (full history on every ref, not just the working tree) for:

- known key-shaped patterns: `AIza…` (Google), `sk-…` (OpenAI-style), `ghp_…`/`github_pat_…`
  (GitHub), `AKIA…` (AWS), `xox[baprs]-…` (Slack), `-----BEGIN … PRIVATE KEY-----` blocks
- generic `*_KEY`/`*_SECRET`/`*_TOKEN`/`password`/`passwd` assignments to a quoted value of 12+
  characters, excluding the known placeholder strings in `.env.example`
- connection strings with embedded credentials (`postgres://user:pass@…` and similar)
- npm/yarn registry auth tokens in lockfile history
- every file ever added anywhere in history whose name matches `.env*`, `*.pem`, `*.key`,
  `*.p12`, `*.pfx`, or contains `secret`/`credential`

**Result: clean.** The only `.env*` file ever committed is `.env.example`, and it has always
held only the placeholder values `GEMINI_API_KEY="MY_GEMINI_API_KEY"` and
`APP_URL="MY_APP_URL"` (AI-Studio boilerplate, never a real value). No other pattern matched
anywhere in history. No maintainer interrupt required.

## Fabrication sweep (E0.2)

Searched every function in `backend/watchdog_api/analytics/` and `backend/watchdog_api/sources/`
for a value returned without being computed from its inputs. Findings:

- **No fabricated scientific value exists.** `analytics/stats.ts` (`pearson`, `spearman`,
  `calculateRatio`, `normalizeMax`) and `analytics/jh16.ts` (`Pi`, `Hi`) all compute from their
  arguments; none has a hardcoded numeric fallback. Verified by a new test asserting `pearson`/
  `spearman` vary with their inputs and return `null` (never a fabricated `0`) on a zero-variance
  input, and that `calculateRatio` returns `null` (never `0` or `Infinity`) for a non-positive
  denominator — `tests/unit/anti_fabrication.test.ts`.
- **Fixture adapters are not fabrication.** `offline_fixture.ts`, `serp.ts`, `google_trends.ts`
  return hardcoded mock payloads, but each is registered with honest `status: 'fixture'` — never
  `'implemented'` — matching `05_PROVIDERS_AND_CAPABILITIES.md`'s own model ("fixture is
  offline-only"). This is the sanctioned pattern (see E1.8's `FixtureSourceAdapter`), not the
  anti-pattern rule 1 is aimed at.
- **Real gap found and fixed:** `sources/registry.ts`'s `getAdapter()` threw a plain `Error` for
  `planned`/`blocked` sources, not the `NotImplementedError` CLAUDE.md rule 1 names explicitly.
  Added `NotImplementedError` to `utils/errors.ts` and wired it into `getAdapter()`. Every
  currently-registered `planned`/`blocked-by-license/auth` source (`pubchem`,
  `scientific_literature`, `erowid`, `drug_checking`, `manual_dataset`) now throws it, asserted by
  `tests/unit/anti_fabrication.test.ts`.
- **Not a fabrication, but adjacent, left for E1.3:** `db/repositories/data.ts`'s
  `source_adapter_version: 'unknown'` on read (already flagged above) is honest about not
  knowing the value — it doesn't invent a fake version — so it doesn't violate rule 1, but the
  underlying data loss (column missing from `observations`) is a real defect for E1.3.
- **Not fixed here, deferred to E1.10:** the `Sources` page and `sourceRegistry.listSources()`
  return `planned`/`blocked` entries alongside `fixture` ones with no adapter, and the frontend
  renders an "Acquire" button for all of them uniformly; clicking one for a `planned` source
  round-trips to the API and fails with `NotImplementedError` server-side rather than being
  disabled client-side. E1.10's test ("a `planned` provider cannot be selected for a run; the UI
  does not render it as available") owns this, not E0.2 — E0.2 is about fabricated *values*, and
  no fabricated value is produced by this flow, only a clickable dead end.

## Diagnostics contract (`06_DIAGNOSTICS.md`) — closer to done than the ledger suggests

`utils/tracer.ts` + `utils/redaction.ts` + `utils/errors.ts` + `api/middleware.ts` together
already implement: four-mode switch via `WATCHDOG_DIAGNOSTICS_MODE`, `AsyncLocalStorage`
correlation context with `trace_id`/`span_id`/`parent_span_id`/monotonic `sequence_no`,
JSONL persistence per trace under `diagnostics/<date>/<trace_id>/`, redaction before every
write, error cause-chain preservation, and context propagation across `runWithSpan` boundaries
(tested end to end in `tracer.test.ts`). Missing: the full `STEP_ENTER→STATE_BEFORE→INPUT→
VALIDATION→DECISION→...` event vocabulary (only a subset of event types are emitted), the
diagnostic bundle (E1.7), and the Debug Console UI. This is a genuine partial credit toward
E1.4-E1.6, reflected in E0.5.

## Config contract (`01_ARCHITECTURE.md`/E1.1) — working but disconnected

`config/loader.ts` + `config/canonicalize.ts` + `config/schemas.ts` pass all three tests in
`config.test.ts` (key-order-independent hashing, locked-preset override rejection, fail-fast
schema validation). Nothing in `server.ts` or `api/routes.ts` calls `ConfigLoader`, so the
running application never actually goes through it — every run today uses whatever the HTTP
request body contains, unvalidated by this system. Flagged for E0.5/E1.1.

## What E0.1 does not do

Per the ledger, E0.1 is inventory, not repair. No source file was modified to produce this
document. Fabrication handling is E0.2, the secrets scan is E0.3, making `test:all` green is
E0.4 (already green — see above), and ticking the ledger is E0.5.
